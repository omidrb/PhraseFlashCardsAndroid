plugins { id 'com.android.application' }
android { namespace 'com.phrasecards.app'; compileSdk 35
 signingConfigs { zope { storeFile file('zope-update.jks'); storePassword 'ZopeFlashCards2026'; keyAlias 'zope'; keyPassword 'ZopeFlashCards2026' } }
 defaultConfig { applicationId "com.phrasecards.app"; minSdk 24; targetSdk 35; versionCode 20; versionName "8.8"; signingConfig signingConfigs.zope } }
dependencies { implementation 'androidx.appcompat:appcompat:1.7.0'; implementation 'com.google.android.material:material:1.12.0' }
